{
  "name": "inference-sim",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter @inference-sim/web dev",
    "test": "pnpm --filter @inference-sim/core build && pnpm -r test",
    "sim": "node packages/cli/dist/main.js"
  }
}
